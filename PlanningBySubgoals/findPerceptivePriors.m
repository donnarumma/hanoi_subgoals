function perceptive_priors=findPerceptivePriors(transition_cpt,goal_state)
% function perceptive_priors=findPerceptivePriors(transition_cpt,goal_state)
% to do: build a function from transition_cpt to getThreePegEncoding
perceptive_priors=init_perceptive_subgoal_priors(goal_state);